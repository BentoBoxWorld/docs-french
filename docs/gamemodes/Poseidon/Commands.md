# Commandes Poseidon

Voir la documentation BSkyBlock pour les commandes complètes. Poseidon utilise les mêmes commandes avec le préfixe `poseidon` à la place de `bskyblock`, et la commande joueur par défaut est `/poseidon` ou `/po` au lieu de `/island`.

# Commandes CaveBlock

Voir la documentation BSkyBlock pour les commandes complètes. CaveBlock utilise les mêmes commandes avec le préfixe `caveblock` à la place de `bskyblock`, et la commande joueur par défaut est `/cave` au lieu de `/island`.

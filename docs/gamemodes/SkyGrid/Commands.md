# Commandes SkyGrid

Voir la documentation BSkyBlock pour les commandes complètes. SkyGrid utilise les mêmes commandes avec le préfixe `skygrid` à la place de `bskyblock`, et la commande joueur par défaut est `/skygrid` ou `/sg` au lieu de `/island`.
